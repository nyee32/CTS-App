package ctsWo

import (
	"fmt"
)

func EnduranceMiles (power, hr int) {
	fmt.Printf("EnduranceMiles Func\n")
	var ctsPower []int
	var ctsHr []int

	ctsPower = append (ctsPower, power * 0.45)
	ctsPower = append (ctsPower, power * 0.73)

	ctsHr = append (ctsHr, hr * 0.5)
	ctsHr = append (ctsHr, hr * 0.91)

	//	return ctsPower, ctsHr
}
